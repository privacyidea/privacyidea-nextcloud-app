/.idea/
/*.iml

/vendor/
/vendor-bin/*/vendor/

/.php-cs-fixer.cache
/tests/.phpunit.cache

/node_modules/
# composer.lock and vendor-bin/*/composer.lock are committed for reproducible
# CI (the dependency set is dev/CI tooling only; nothing ships in vendor/).
/package-lock.json
