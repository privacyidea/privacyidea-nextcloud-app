; Dev-only: make PHP pick up edited source immediately (the app is bind-mounted),
; instead of caching it for opcache.revalidate_freq seconds. Do not ship this.
opcache.validate_timestamps=1
opcache.revalidate_freq=0
